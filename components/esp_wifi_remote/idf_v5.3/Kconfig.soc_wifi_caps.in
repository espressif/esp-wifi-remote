# This file is auto-generated

if SLAVE_IDF_TARGET_ESP32

    config SLAVE_SOC_WIFI_SUPPORTED # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_WAPI_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_CSI_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_MESH_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_SUPPORT_VARIABLE_BEACON_WINDOW # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_NAN_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_IDF_TARGET_ARCH_XTENSA
        bool
        default y

endif # ESP32

if SLAVE_IDF_TARGET_ESP32S2

    config SLAVE_SOC_WIFI_SUPPORTED # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_LIGHT_SLEEP_CLK_WIDTH # kconfig ignore: multiple-definition
        int
        default 12

    config SLAVE_SOC_WIFI_HW_TSF # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_FTM_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_WAPI_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_CSI_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_MESH_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_SUPPORT_VARIABLE_BEACON_WINDOW # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_NAN_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_FREERTOS_UNICORE
        bool
        default y

    config SLAVE_IDF_TARGET_ARCH_XTENSA
        bool
        default y

endif # ESP32S2

if SLAVE_IDF_TARGET_ESP32C3

    config SLAVE_SOC_WIFI_SUPPORTED # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_LIGHT_SLEEP_CLK_WIDTH # kconfig ignore: multiple-definition
        int
        default 12

    config SLAVE_SOC_WIFI_HW_TSF # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_FTM_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_GCMP_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_WAPI_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_CSI_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_MESH_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_SUPPORT_VARIABLE_BEACON_WINDOW # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_PHY_NEEDS_USB_WORKAROUND # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_FREERTOS_UNICORE
        bool
        default y

    config SLAVE_IDF_TARGET_ARCH_RISCV
        bool
        default y

endif # ESP32C3

if SLAVE_IDF_TARGET_ESP32S3

    config SLAVE_SOC_WIFI_SUPPORTED # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_LIGHT_SLEEP_CLK_WIDTH # kconfig ignore: multiple-definition
        int
        default 12

    config SLAVE_SOC_WIFI_HW_TSF # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_FTM_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_GCMP_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_WAPI_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_CSI_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_MESH_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_SUPPORT_VARIABLE_BEACON_WINDOW # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_PHY_NEEDS_USB_WORKAROUND # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_IDF_TARGET_ARCH_XTENSA
        bool
        default y

endif # ESP32S3

if SLAVE_IDF_TARGET_ESP32C2

    config SLAVE_SOC_WIFI_SUPPORTED # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_LIGHT_SLEEP_CLK_WIDTH # kconfig ignore: multiple-definition
        int
        default 12

    config SLAVE_SOC_WIFI_HW_TSF # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_FTM_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_SUPPORT_VARIABLE_BEACON_WINDOW # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_PHY_NEEDS_USB_WORKAROUND # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_FREERTOS_UNICORE
        bool
        default y

    config SLAVE_IDF_TARGET_ARCH_RISCV
        bool
        default y

endif # ESP32C2

if SLAVE_IDF_TARGET_ESP32C6

    config SLAVE_SOC_WIFI_SUPPORTED # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_LIGHT_SLEEP_CLK_WIDTH # kconfig ignore: multiple-definition
        int
        default 12

    config SLAVE_SOC_WIFI_HW_TSF # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_FTM_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_GCMP_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_WAPI_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_CSI_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_MESH_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_SOC_WIFI_HE_SUPPORT # kconfig ignore: multiple-definition
        bool
        default y

    config SLAVE_FREERTOS_UNICORE
        bool
        default y

    config SLAVE_IDF_TARGET_ARCH_RISCV
        bool
        default y

endif # ESP32C6
